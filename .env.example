# Garmin Connect credentials
USERNAME=your@email.com
PASSWORD=yourpassword

# Optional overrides
# GARMIN_START_DATE=2015-01-01    # earliest date to pull (default: 2010-01-01)
# DB_PATH=garmin.db               # SQLite file location (default: garmin.db)
# GARMIN_TOKEN_STORE=.garth       # directory for cached OAuth tokens
